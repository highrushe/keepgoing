<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prac.keepgoing.DBMapper">
<!-- id : DAO나 Controller등 Java내에서 호출할때 쓰임. -->
<!-- resultType : 결과 값의 자료형 또는 Bean(컬럼, 자료형). mybatis 설정에서 Alias를 취해준 것 -->
	<select id='test' resultType="Test">
		select * from test;
	</select>
</mapper>